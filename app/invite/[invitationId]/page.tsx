"use client";
import { useEffect, useState } from "react";
import { useRouter } from "next/navigation";
import Link from "next/link";
import { authClient } from "@/app/lib/auth-client";
import { Button } from "@/components/ui/button";
export default function InvitePage({ params }: { params: Promise<{ invitationId: string }> }) { const router = useRouter(); const [state, setState] = useState<any>({ loading: true }); useEffect(() => { void params.then(async ({ invitationId }) => { const invite = await (authClient.organization as any).getInvitation({ query: { id: invitationId } }); const session = await authClient.getSession(); if (invite.error) setState({ loading: false, message: "This invitation is expired, cancelled, or already accepted." }); else if (!session.data) setState({ loading: false, invite: invite.data, message: "Sign in with the invited email to continue." }); else if (session.data.user.email.toLowerCase() !== invite.data.email.toLowerCase()) setState({ loading: false, invite: invite.data, mismatch: true, message: `This invitation is for ${invite.data.email}.` }); else setState({ loading: false, invite: invite.data }); }); }, [params]); const accept = async () => { const result = await (authClient.organization as any).acceptInvitation({ invitationId: state.invite.id }); if (result.error) setState({ ...state, message: result.error.message }); else router.push(`/${state.invite.organization?.slug || ""}`); }; const decline = async () => { const result = await (authClient.organization as any).rejectInvitation({ invitationId: state.invite.id }); setState({ ...state, message: result.error?.message || "Invitation declined." }); }; if (state.loading) return <main className="auth-shell"><p>Loading invitation…</p></main>; return <main className="auth-shell"><section className="auth-card"><h1>Workspace invitation</h1><p>{state.message}</p>{state.invite && !state.mismatch && (state.message?.startsWith("Sign in") ? <Link href={`/login?redirect=/invite/${state.invite.id}`}>Sign in</Link> : <><Button onPress={accept}>Accept invitation</Button><Button variant="outline" onPress={decline}>Decline</Button></>)}{state.mismatch && <Link href={`/login?redirect=/invite/${state.invite.id}`}>Sign in as the invited account</Link>}</section></main>; }
